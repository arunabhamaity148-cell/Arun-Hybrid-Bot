"""
data/ — Arunabha Hybrid Bot v1.0 Data Package
  binance_client.py   — Binance USD-M Futures REST + WebSocket. NEVER places orders.
  coingecko_client.py — Trending, MarketCap, Fear & Greed.
  coindcx_client.py   — CoinDCX price reference (read-only).
  delta_client.py     — Delta Exchange READ-ONLY price feed.
  cache_manager.py    — Async in-memory TTL cache.
"""
